# ============================================================
# SKYDE.cfg - CONFIGURACAO DE PERFIS AVANCADA (incluida pelo master)
# Pode conter perfis adicionais ou opcoes que se sobrepoem.
# Exemplo: parametros globais de ligacao a servidores de leitura.
# ============================================================

# Servidor de leitura (reader) - exemplo newcamd:
# SERVER: host porta user pass { protocol=newcamd; deskey=... }
# SERVER: host porta user pass { protocol=cccam }
# SERVER: host porta user pass { protocol=mgcamd }

# Exemplo de perfil com reader:
#[Meu Reader]
#PORT: 15002
#CAID: 0100
#SERVER: 192.168.1.100 34000 readeruser readerpass { protocol=newcamd }
